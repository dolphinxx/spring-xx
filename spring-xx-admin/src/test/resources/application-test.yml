spring:
  datasource:
    url: jdbc:h2:mem:xx;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=false;NON_KEYWORDS=KEY,VALUE;MODE=MYSQL;DATABASE_TO_LOWER=TRUE;CASE_INSENSITIVE_IDENTIFIERS=TRUE;
    driver-class-name: org.h2.Driver
    name: xx
  sql:
    init:
      schema-locations: classpath:init/schema.sql