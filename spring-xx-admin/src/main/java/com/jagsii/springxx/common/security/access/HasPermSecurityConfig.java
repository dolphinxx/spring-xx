package com.jagsii.springxx.common.security.access;

import org.springframework.security.access.SecurityConfig;

public class HasPermSecurityConfig extends SecurityConfig {
    public HasPermSecurityConfig(String config) {
        super(config);
    }
}
