spring:
  profiles:
    active: test
#  datasource:
#    type: org.springframework.jdbc.datasource.embedded.EmbeddedDatabaseFactory.EmbeddedDataSourceProxy
#    url: jdbc:h2:mem:xx;DB_CLOSE_DELAY=-1;NON_KEYWORDS=KEY,VALUE
#    driver-class-name: org.h2.Driver
logging:
  level:
    root: debug